const heroImg = require.context('../assets', true ); // Obtiene todos los archivos del directorio
// y el parametro true, es para que revise tambien en los subdirectorios.

// La función es de WepPack

export default heroImg;
